{
    "name": "tu-usuario/to-do-list",
    "autoload": {
        "psr-4": {
            "App\\": "src/"
        }
    },
    "require-dev": {
        "phpunit/phpunit": "^10.0"
    },
    "scripts": {
        "test": "phpunit",
        "test:unit": "phpunit tests/unit",
        "test:watch": "phpunit --watch"
    }
}